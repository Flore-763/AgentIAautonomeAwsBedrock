# ============================================================
# CORE - AWS & BOTO3
# ============================================================
boto3>=1.35.0,<2.0.0
botocore>=1.35.0,<2.0.0

# ============================================================
# OPENSEARCH
# ============================================================
opensearch-py>=2.6.0,<3.0.0
requests-aws4auth>=1.2.3,<2.0.0

# ============================================================
# LANGCHAIN ECOSYSTEM (v0.3.x - stable)
# ============================================================
langchain>=0.3.0,<0.4.0
langchain-core>=0.3.0,<0.4.0
langchain-aws>=0.2.0,<0.3.0
langchain-text-splitters>=0.3.0,<0.4.0

# ============================================================
# LANGGRAPH
# ============================================================
# Version relevée par rapport à l'ancienne (0.2.x) : `ToolExecutor` est
# retiré de langgraph.prebuilt dans les versions récentes (remplacé par
# le tool calling natif + ToolNode). Cette plage couvre les versions où
# StateGraph, add_messages, MemorySaver et bind_tools sont stables.
langgraph>=0.2.28,<0.4.0
langgraph-checkpoint>=1.0.0,<3.0.0

# ============================================================
# PDF PROCESSING (indexation des documents internes)
# ============================================================
pypdf>=4.0.0,<5.0.0
openpyxl>=3.1.0,<4.0.0
python-docx>=1.1.0,<2.0.0
# pandas>=2.0.0,<3.0.0
# ============================================================
# APPELS HTTP (outil météo -> Open-Meteo)
# ============================================================
requests>=2.32.0,<3.0.0

# ============================================================
# TEXT PROCESSING & UTILITAIRES
# ============================================================
tiktoken>=0.7.0,<0.8.0
typing-extensions>=4.12.0,<5.0.0
python-dateutil>=2.9.0,<3.0.0

# ============================================================
# AUTHENTIFICATION (vérification des JWT Cognito côté server.py)
# ============================================================
python-jose[cryptography]>=3.3.0,<4.0.0

# ============================================================
# DÉPENDANCES INDIRECTES (pour éviter les conflits de résolution)
# ============================================================
urllib3>=1.26.0,<2.0.0
pydantic>=2.0.0,<3.0.0
anyio>=4.0.0,<5.0.0
fastapi
uvicorn
# python-dotenv==1.0.0  

