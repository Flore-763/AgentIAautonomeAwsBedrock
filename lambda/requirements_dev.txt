# Dépendances de TEST uniquement — jamais installées en production
# (le Dockerfile ne lit que requirements.txt, pas ce fichier).
pytest>=8.0.0,<9.0.0
pytest-cov>=5.0.0,<6.0.0
pytest-mock>=3.14.0,<4.0.0